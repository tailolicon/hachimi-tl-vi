# Canonical finding completion — Chasing After You

Claim: `canonical-findings-maintenance-auto11-20260903T100926Z`
Finding: `cf-04c407aa449b0c6e` (`逐君之形` / `アナタヲ・オイカケテ`)

## Durable resolution

- Exact-scoped canonical hardener is live on `main` for `text_data_dict.json`, category `147`, source alias `逐君之形`, preferred/accepted target **Chasing After You**, `match_mode: exact`, item-scoped invalidation.
- The corresponding terminology-review lock uses the verified JP identity `アナタヲ・オイカケテ` and English-release title **Chasing After You**.
- Regression coverage proves the intended finding resolves to the community term and proves negative scope for category `144` and `localize_dict.json`.
- Validate workflow run `33741150697` completed successfully.
- Production `Sync translation context` run `33740369926`, latest-attempt job `100608883020`, completed successfully. The workflow ran all hardeners/resolvers, canonical refresh, generated-context rebuild and **513 tests**, then pushed generated context to `main` at commit `c3e4248ed6c9b0e1eee587b2792b384144030197`.
- Live repository community/review evidence exposes **Chasing After You** as the canonical Skill identity. Repository-wide verification finds no canonical/glossary lock for bare `YOUR SHADOW`.
- The requested `Chasing After You Ⅲ` check found no independent canonical title record requiring a second lock; the canonical decision remains exact to the base Skill identity rather than broadening matching to arbitrary suffixes. This preserves the negative-scope guarantee instead of inventing a prefix rule.

## Result

The prior proper-name uncertainty is durably resolved by the verified official English Skill identity, with exact source/category/path scope and production-generated context synchronized. This finding is complete; continue the maintenance lane with the next live unresolved finding.
