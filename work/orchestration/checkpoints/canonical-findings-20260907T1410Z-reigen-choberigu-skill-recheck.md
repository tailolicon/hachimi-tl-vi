# Canonical finding recheck — 灼然灵验无敌棒神谕

Finding: `cf-62b546f275e9315f` (`灼然灵验无敌棒神谕`), a Skill-title finding reused inside category-172 inheritance descriptions for character family `1004`.

Live `scripts/canonical_findings.py::active_findings` still exposes this finding as blocking. Fresh identity research confirms that character `100403` is `[祝ひ寿ぐ神速天女] Maruzensky` and the JP-only unique Skill is `霊験灼然チョベリグ神託` (Skill id `120041`). The current Global-facing community reference explicitly marks both this costume and Skill as JP-only, so there is no official Global English Skill title to import as canonical identity yet.

The zh-CN source is therefore an alias/translation of a verified JP Skill identity, but promoting the current Vietnamese `Thần Dụ Rực Cháy Linh Nghiệm Vô Địch` or inventing a new calque would still guess the release title rather than lock an authoritative player-facing name.

Decision: keep `cf-62b546f275e9315f` unresolved/deferred until authoritative Global identity exists (or repository policy explicitly adopts a Vietnamese localization for JP-only Skill titles). Do not increment `completed_count`. Continue to the next live active blocker.
